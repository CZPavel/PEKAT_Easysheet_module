# Changelog

Form?t vych?z? z Conventional Commits.

## Unreleased

### Added
- Spreadsheet mapping workspace s Context JSON drag-and-drop bindingy.
- Workbook API a bezpe?n? formula subset pro `PV`, `PV_COUNT`, `IF`, `AND`, `OR`.
- React/Vite offline UI pro n?hled bez PEKAT instance.
- Offline PEKAT simulator s demo kamerami a tick endpointy.

## 0.1.0 - 2026-05-28

### Added
- Spreadsheet mapping workspace s Context JSON drag-and-drop bindingy.
- Workbook API a bezpe?n? formula subset pro `PV`, `PV_COUNT`, `IF`, `AND`, `OR`.
- Bootstrap projektu `PEKAT_Easysheet_module`.
- FastAPI MVP backend s registrac? projekt?, snapshoty a evaluac?.
- PEKAT Code bridge s timeoutem a bezpe?n?m fallbackem.
- Smoke testy a z?kladn? dokumentace.
